import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, Link } from '@inertiajs/react';
import { AlertTriangle, ArrowRight, Boxes, CircleDollarSign, FilePlus2, PackageCheck, Plus, Receipt, Ship, TrendingDown, TrendingUp, WalletCards } from 'lucide-react';
import { Bar, BarChart, CartesianGrid, Cell, ComposedChart, LabelList, Legend, Line, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';

const money=(value:number)=>new Intl.NumberFormat('fr-FR',{maximumFractionDigits:0}).format(value)+' Ar';
const compact=(value:number)=>new Intl.NumberFormat('fr-FR',{notation:'compact',maximumFractionDigits:1}).format(value);
const colors=['#BD2433','#FCF108','#2F2F2F','#6B7280','#16A34A','#F97316','#2563EB'];
const revenueColors=['#FCF108','#E7DA0D','#D4C51A','#C89222','#C2562C','#BD2433'];
const EmptyChart=({label}:{label:string})=><div className="grid h-full place-items-center text-center text-sm text-gray-400">{label}</div>;
const RevenueBar=({x=0,y=0,width=0,height=0,fill='#BD2433'}:any)=>{const roof=Math.min(13,width*.34,height*.3);return <path d={`M ${x} ${y+roof} L ${x+width/2} ${y} L ${x+width} ${y+roof} L ${x+width} ${y+height} L ${x} ${y+height} Z`} fill={fill}/>;};

export default function Dashboard({metrics,counts,chart,orderStatus,expenseCategories,topProducts}:{metrics:any;counts:any;chart:any[];orderStatus:any[];expenseCategories:any[];topProducts:any[]}){
 const cards=[['CA facturé',metrics.invoiced,'Ce mois',TrendingUp,'red'],['CA encaissé',metrics.received,'Ce mois',WalletCards,'yellow'],['Profit net',metrics.profit,'Après charges',CircleDollarSign,'charcoal'],['Solde de trésorerie',metrics.cash,'Disponible',Receipt,'green'],['Commission encaissée',metrics.commission,'Services',TrendingUp,'red'],['Dépenses business',metrics.business,'Ce mois',TrendingDown,'yellow']];
 return <AuthenticatedLayout header={<><p className="eyebrow">Vue d’ensemble</p><h1 className="page-title">Bonjour, voici votre activité</h1></>} action={<Link href="/modules/commandes/create" className="btn-primary"><Plus size={17}/>Nouvelle commande</Link>}>
  <Head title="Dashboard"/>
  <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([label,value,note,Icon,tone]:any)=><div className="metric-card" key={label}><div className={`metric-icon ${tone}`}><Icon size={20}/></div><div><p className="text-xs font-medium text-gray-500">{label}</p><strong className="mt-1 block text-xl">{money(value)}</strong><small className="text-[11px] text-gray-400">{note}</small></div></div>)}</div>

  <div className="mt-5 grid gap-5 xl:grid-cols-[1.65fr_1fr]">
   <section className="panel"><div className="panel-head"><div><h2>Chiffre d’affaires</h2><p>Progression mensuelle facturée et encaissée</p></div><span className="badge">6 derniers mois</span></div><div className="h-[310px] pt-5"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={chart} margin={{top:22,right:12,left:2,bottom:0}}><CartesianGrid strokeDasharray="3 5" vertical={false} stroke="#ECECE7"/><XAxis dataKey="month" tickLine={false} axisLine={false} tick={{fontSize:11,fill:'#777',fontFamily:'inherit'}}/><YAxis tickFormatter={compact} tickLine={false} axisLine={false} tick={{fontSize:10,fill:'#999',fontFamily:'inherit'}}/><Tooltip formatter={(value:any,name:any)=>[money(Number(value)),name]} contentStyle={{borderRadius:12,border:'1px solid #E8E7E2',boxShadow:'0 12px 30px rgba(47,47,47,.12)',fontFamily:'inherit'}}/><Legend iconType="circle" wrapperStyle={{fontSize:11,paddingTop:8,fontFamily:'inherit'}}/><Bar name="CA facturé" dataKey="facture" shape={<RevenueBar/>} maxBarSize={46}>{chart.map((_,index)=><Cell key={index} fill={revenueColors[index%revenueColors.length]}/>)}<LabelList dataKey="facture" position="top" formatter={(value:any)=>compact(Number(value))} style={{fontSize:10,fontWeight:700,fill:'#555',fontFamily:'inherit'}}/></Bar><Line name="CA encaissé" type="monotone" dataKey="encaisse" stroke="#BD2433" strokeWidth={2.5} strokeDasharray="5 4" dot={{r:4,fill:'#fff',stroke:'#BD2433',strokeWidth:2}} activeDot={{r:5}}/></ComposedChart></ResponsiveContainer></div></section>
   <section className="panel"><div className="panel-head"><div><h2>Actions urgentes</h2><p>À traiter aujourd’hui</p></div><span className="grid size-8 place-items-center rounded-lg bg-red-50 text-[#BD2433]"><AlertTriangle size={17}/></span></div><div className="divide-y divide-gray-100">{[[Ship,'Colis en transit',counts.shipments,'logistique'],[PackageCheck,'Commandes en cours',counts.orders,'commandes'],[Boxes,'Produits en stock faible',counts.lowStock,'stock']].map(([Icon,label,total,href]:any)=><Link href={'/modules/'+href} className="group flex items-center gap-3 py-4" key={label}><span className="grid size-10 place-items-center rounded-xl bg-[#F8F8F6] text-gray-600"><Icon size={18}/></span><span className="flex-1"><strong className="block text-sm">{label}</strong><small className="text-gray-400">{total} élément(s)</small></span><ArrowRight size={16} className="text-gray-300 group-hover:text-[#BD2433]"/></Link>)}</div></section>
  </div>

  <div className="mt-5 grid gap-5 xl:grid-cols-2">
   <section className="panel"><div className="panel-head"><div><h2>Flux de trésorerie</h2><p>Encaissements, sorties et résultat mensuel</p></div><span className="badge">6 mois</span></div><div className="h-[280px] pt-5"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#eee"/><XAxis dataKey="month" tickLine={false} axisLine={false}/><YAxis tickFormatter={compact} tickLine={false} axisLine={false}/><Tooltip formatter={(value:any)=>money(Number(value))}/><Legend/><Bar name="Encaissements" dataKey="encaisse" fill="#16A34A" radius={[6,6,0,0]}/><Bar name="Sorties" dataKey="depenses" fill="#BD2433" radius={[6,6,0,0]}/><Bar name="Résultat net" dataKey="net" fill="#FCF108" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></section>
   <section className="panel"><div className="panel-head"><div><h2>Produits les plus commandés</h2><p>Classement par quantité commandée</p></div></div><div className="h-[280px] pt-5">{topProducts.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={topProducts} layout="vertical" margin={{left:15}}><CartesianGrid strokeDasharray="3 3" horizontal={false} stroke="#eee"/><XAxis type="number" tickLine={false} axisLine={false}/><YAxis type="category" dataKey="name" width={120} tickLine={false} axisLine={false} tick={{fontSize:11}}/><Tooltip formatter={(value:any)=>[Number(value).toLocaleString('fr-FR'),'Quantité']}/><Bar dataKey="quantity" fill="#2F2F2F" radius={[0,6,6,0]}/></BarChart></ResponsiveContainer>:<EmptyChart label="Les produits commandés apparaîtront ici."/>}</div></section>
  </div>

  <div className="mt-5 grid gap-5 xl:grid-cols-2">
   <section className="panel"><div className="panel-head"><div><h2>Commandes par statut</h2><p>Répartition de toutes les commandes actives</p></div></div><div className="h-[290px] pt-4">{orderStatus.length?<ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={orderStatus} dataKey="value" nameKey="name" innerRadius={58} outerRadius={92} paddingAngle={3}>{orderStatus.map((_,index)=><Cell key={index} fill={colors[index%colors.length]}/>)}</Pie><Tooltip formatter={(value:any)=>[Number(value).toLocaleString('fr-FR'),'Commandes']}/><Legend verticalAlign="bottom"/></PieChart></ResponsiveContainer>:<EmptyChart label="Aucune commande disponible."/>}</div></section>
   <section className="panel"><div className="panel-head"><div><h2>Dépenses par catégorie</h2><p>Répartition des sorties du mois courant</p></div></div><div className="h-[290px] pt-4">{expenseCategories.length?<ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={expenseCategories} dataKey="value" nameKey="name" innerRadius={58} outerRadius={92} paddingAngle={3}>{expenseCategories.map((_,index)=><Cell key={index} fill={colors[(index+2)%colors.length]}/>)}</Pie><Tooltip formatter={(value:any)=>money(Number(value))}/><Legend verticalAlign="bottom"/></PieChart></ResponsiveContainer>:<EmptyChart label="Aucune dépense pour ce mois."/>}</div></section>
  </div>

  <section className="mt-5 panel"><div className="panel-head"><div><h2>Raccourcis</h2><p>Enregistrez rapidement une opération</p></div></div><div className="grid gap-3 pt-4 sm:grid-cols-2 lg:grid-cols-4">{[[PackageCheck,'Créer une commande','commandes'],[FilePlus2,'Créer un devis','devis'],[WalletCards,'Saisir un paiement','paiements'],[Receipt,'Ajouter une dépense','depenses']].map(([Icon,label,href]:any)=><Link href={`/modules/${href}/create`} className="quick" key={label}><Icon size={20}/><span>{label}</span><ArrowRight size={15} className="ml-auto"/></Link>)}</div></section>
 </AuthenticatedLayout>;
}
