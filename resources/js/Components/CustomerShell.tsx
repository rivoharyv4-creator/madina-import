import PublicLayout, { PublicConfig } from '@/Layouts/PublicLayout';
import { Head, Link, usePage } from '@inertiajs/react';
import { PropsWithChildren, ReactNode } from 'react';
import { CheckCircle2, CircleAlert } from 'lucide-react';
export default function CustomerShell({ title, publicConfig, children, header }: PropsWithChildren<{ title: string; publicConfig: PublicConfig; header?: ReactNode }>) {
    const { flash, errors, auth } = usePage().props as any;
    return <PublicLayout config={publicConfig}><Head title={title}/><section className="public-section"><div className="public-container commerce">{header ?? <><div className="mb-6 flex flex-wrap gap-4 text-sm"><Link href="/catalogue">Catalogue</Link><Link href="/panier">Panier</Link><Link href="/mes-commandes">Mes commandes</Link>{auth?.user ? <Link href="/logout" method="post" as="button">Déconnexion</Link> : <Link href="/connexion">Connexion client</Link>}</div><h1 className="mb-8 text-3xl font-extrabold">{title}</h1></>}{flash?.success && <div role={flash.success.includes('indisponible') ? 'alert' : 'status'} className={`mx-auto mb-6 flex max-w-lg items-start gap-3 rounded-2xl border px-5 py-4 text-sm leading-6 ${flash.success.includes('indisponible') ? 'border-red-400/25 bg-red-400/5' : 'border-emerald-500/20 bg-emerald-500/[.06]'}`}>{flash.success.includes('indisponible') ? <CircleAlert size={20} className="mt-0.5 shrink-0 text-red-500" aria-hidden="true" /> : <CheckCircle2 size={20} className="mt-0.5 shrink-0 text-emerald-600" aria-hidden="true" />}<p className="font-medium">{flash.success}</p></div>}{Object.values(errors || {}).length > 0 && <div role="alert" className="mb-5 rounded border border-red-500 p-4">{Object.values(errors).map((error: any, i) => <p key={i}>{error}</p>)}</div>}{children}</div></section></PublicLayout>;
}
